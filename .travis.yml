language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t balazsprehoda/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true balazsprehoda/react-test npm test

after_success:
  - docker build -t balazsprehoda/multi-client ./client
  - docker build -t balazsprehoda/multi-nginx ./nginx
  - docker build -t balazsprehoda/multi-server ./server
  - docker build -t balazsprehoda/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push balazsprehoda/multi-client
  - docker push balazsprehoda/multi-nginx
  - docker push balazsprehoda/multi-server
  - docker push balazsprehoda/multi-worker
